/** Search radius for nearby ambulances / auto-dispatch candidate pool (project plan §7). */
export const DEFAULT_DISPATCH_SEARCH_RADIUS_KM = 10;

/** Driver must accept ride request within this window (project plan §5.4). */
export const DRIVER_ACCEPT_TIMEOUT_SECONDS = 15;
